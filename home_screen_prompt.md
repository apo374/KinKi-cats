# 住宅向けホーム画面の実装プロンプト

以下は、今のホーム画面の完成状態を再現するための「一発指示用プロンプト」です。これは他のページや既存の構造を壊さず、ホーム画面とその関連機能だけを意図どおりに再実装するための仕様です。

---

## 指示本文

以下の要件をすべて満たすように、Flask + Jinja + CSS + Leaflet を使ったホーム画面を実装してください。

この実装は、ホーム画面専用の UI と関連機能に限定してください。他のページや別の画面の構成を変更しないでください。既存の他画面のコードに影響を与えないようにしてください。

### 1. 目的

この画面は、青森市の防災情報を一目で把握できる「ホームダッシュボード」です。ユーザーは、緊急情報、気象警報、住民向けのお知らせ、避難所、被害情報を迅速に確認できる必要があります。

また、地図から避難所や発信情報を見て、現地の状況を把握しやすくすることを目的とします。

---

### 2. 全体のレイアウト

ページは、上から下に以下の順で構成してください。

1. ヘッダー
2. 緊急情報バナー
3. メインコンテンツ
   - 左側: 地図セクション
   - 右側: 気象情報 + お知らせ
4. 地図下部の操作ボタン群

全体として、ホームページの画面は「情報の見出しとカード化」が明確で、落ち着いた青系のデザインを採用してください。

---

### 3. ヘッダー

- 左側にブランド名「防災アプリ」を表示
- 右側に「全体更新」ボタンを表示
- 「全体更新」ボタンは、画面の更新を行うためのアクションとして機能する
- 画面上部に白〜水色系の背景を使用し、ブランドとナビを際立たせる
- 安定した高さを保ち、モバイルではボタンが画面幅に収まるように調整する

---

### 4. 緊急情報バナー

- 画面の上部に、重要な緊急情報を示すアラートバーを配置
- バックグラウンドは薄いピンク／レッド系
- 左側にラベル「市からの緊急情報」または「緊急情報」
- 右側または中央に本文を表示
- 重要な発令内容がある場合は、それを強調して見せる
- 「詳細を確認」リンクを表示
- もし緊急情報がない場合は非表示にする

---

### 5. メイン列レイアウト

メインエリアは2列レイアウトにしてください。

- 左列: 約 1.7 倍の幅
- 右列: 約 0.9 倍の幅
- 画面幅が小さい場合は縦並びに切り替える

このレイアウトは、地図を大きく見せる構成にすることが重要です。

---

### 6. 左側：地図セクション

#### 6.1 全体構成

- セクション見出し: 「青森市周辺マップ」
- 見出しの前にアイコンを付ける
- 地図が入る枠を 330〜420px 程度の高さで表示
- 地図の右上に検索フォームを重ねる
- 地図の下部左に表示切替チェックボックスを配置
- 地図の下部右に「現在地」ボタンを配置
- 地図の下部に「避難所一覧」へのリンクと「地図を拡大」ボタンを配置

#### 6.2 地図の見た目

- Leaflet を使って地図を表示
- 地図の背景は OpenStreetMap タイルを使用
- 初期ビューは青森市周辺（緯度 40.8222, 経度 140.7474）を中心にし、ズーム 12 で表示
- 避難所は青系円マーカー
- 被害・発信情報は赤系円マーカー
- 避難所、被害・発信の表示は切り替え可能にする
- 現在地の表示は青系の円とマーカーで表現する

#### 6.3 検索機能

- 地図の検索フォームでは「登録地名・名称」で検索できるようにする
- 入力して検索ボタンを押すと、表示中の避難所・発信情報から一致する項目を候補として出す
- 候補をクリックすると地図上に該当箇所が移動し、ポップアップを開く
- 一致しない場合は「表示中の登録情報に一致するものはありません。」を表示
- 検索の候補は、地名と名称の両方を含めて照合する

#### 6.4 フィルタ機能

- 「避難所」チェックボックス
- 「被害・発信」チェックボックス
- チェックを外すと、対応するマーカーを非表示にする
- 再度チェックすると再表示する

#### 6.5 現在地取得

- 「現在地」ボタンを押すと、ブラウザの Geolocation API を使って現在地を取得
- 取得後、地図をその位置に移動し、現在地の範囲を示す円を表示
- 位置情報取得の失敗時は、ユーザーにわかりやすいメッセージを表示
- 取得中はボタンを無効化して、処理中の状態を示す

#### 6.6 地図拡大機能

- 「地図を拡大」ボタンを押すと、地図セクションが拡張表示になる
- 押すたびに ON/OFF を切り替える
- 画面内の地図サイズが大きくなり、表示が見やすくなる
- 拡張状態のときは、「元に戻す」に文言が変わる
- `requestAnimationFrame` を使って地図のサイズ再計算を行う

#### 6.7 フォールバック

- Leaflet が利用できない場合は地図を隠し、代わりに「地図を表示できませんでした。避難所は一覧から確認できます。」を表示
- 地図が使えない場合、検索フォームやフィルタも非表示にする

---

### 7. 右側：気象情報セクション

- 見出し: 「気象警報・注意報」
- 見出しの右側に「10分ごとに自動更新」を表示
- 取得中は読み込み状態を表すメッセージを表示
- データ取得後、最新の警報があればリスト表示
- 警報・注意報が発表されていない場合は「現在、警報・注意報は発表されていません」と表示
- 取得した地域名を表示
- 取得時刻と気象庁発表時刻を表示

#### 7.1 緊急情報連動

- 特定の緊急レベルの警報コードがあれば、ページ上部のバナーに自動設定
- 例: 大雨・暴風・高潮・土砂災害関連の特別警報、危険警報など
- 警報の種類が多い場合は、コードで判定し、名前だけをカンマ区切りで表示

---

### 8. 右側：住民向けのお知らせ

- 見出し: 「住民向けのお知らせ」
- 有効中のお知らせのみを表示
- 各アイテムはカード形式
- 左側に赤い境界線を表示
- 題名や本文、避難先、更新日を表示
- 解除済み・完了済みのデータは表示しない
- 何もない場合は「現在有効な住民向けのお知らせはありません。」と表示

---

### 9. UIデザイン要件

#### 9.1 カラーパレット

- メインの青: `#145a97` / `#327dc2` / `#d8f1ff`
- 緊急色: `#bd3c36` / `#cf4339`
- 背景色: `#f7fafd` / `#fff`
- 境界線: `#dce6f0`
- テキスト: `#203651` / `#576b82`

#### 9.2 文字表現

- 見出しは太めで読みやすく、セクションごとに明確な見出しデザイン
- 日本語の情報は簡潔に、重要度が高い情報ほど太く強調
- 「避難所」「被害・発信」「警報」「更新時刻」などは、意味の伝わりやすさを優先する

#### 9.3 角丸・余白・影

- セクションの角はやや丸くして、クリーンな印象にする
- カードやパネルに少しの影を付けて重なりをつける
- 余白は適度に確保し、情報が詰まりすぎないようにする

---

### 10. 関連動作要件

#### 10.1 自動更新

- 気象警報は 10 分ごとに自動更新
- 地図データは 10 分ごとに再取得
- 画面上部の更新ボタンでも強制更新が可能

#### 10.2 ステータス表示

- 「登録情報を読み込んでいます...」
- 「避難所 X 件・被害・発信 Y 件。表示のない場所も安全とは限りません。」
- 「現在地を表示しました。位置には誤差があります。」
- など、ユーザーに次の状態がわかるようにステータスを表示する

#### 10.3 画面アクセシビリティ

- `aria-live` を使って状態更新を伝える
- 地図とフォームには適切な `aria-label` を付ける
- 検索フォームと結果リストに `role="search"` や `aria-live="polite"` を付ける
- ボタンには視認性の高いラベルと適切なテキストを持たせる

---

### 11. レスポンシブデザイン

以下の挙動を実装してください。

- 900px 以下: 2列レイアウトを縦並びレイアウトに変更
- 760px 以下: 見出しのサイズと余白を小さく調整
- 420px 以下: ボタンやリンクの幅を適切に縮める
- 画面幅が小さい場合でも、地図の高さを維持しつつ、見切れないように十分な余白を確保する

---

### 12. 実装上の制約

- この実装はホームページ専用に限定する
- 既存の他ページのデザインや機能を変更しない
- 既存のデータ構造とファイル構成を壊さない
- 地図データは `api_map_data` に依存する
- 気象情報は `api/weather_warnings` に依存する
- 表示されるデータがない場合でも、UIを崩さず読み込み状態や空状態を自然に出す
- XML/HTML/CSS/JSの途中で壊れないようにする

---

### 13. 実装の正確な完成形

最終的な完成物は、次のような「ちゃんとした防災ホームダッシュボード」になることが期待されます。

- 上部にブランドと更新ボタンがある
- 緊急情報バナーが自然に見える
- 左側に大きい地図があり、避難所、発信情報、現在地の情報が見える
- 右側に気象警報と住民向けお知らせが並ぶ
- 画面全体が落ち着いた防災向け色合いになっている
- 10 分ごとの更新や地図検索・フィルタが機能している
- モバイルでも崩れずに見やすい

---

### 14. 実装対象の技術スタック

- Python Flask
- Jinja2 templates
- CSS for layout and styling
- Leaflet JS for map rendering
- JavaScript for map interactions and auto-refresh

---

### 15. 最終チェックリスト

実装完了前に、次の項目を必ず確認してください。

- ホーム画面だけが変更対象である
- 他のページやルートを壊していない
- 地図の高さ・位置・検索フォーム・フィルタが崩れていない
- 気象情報セクションとお知らせセクションが整列している
- 緊急情報が表示されたときにバナーが見える
- 地図のマーカーが色分けされている
- 現在地ボタンが機能している
- 地図拡大ボタンが機能している
- モバイル時にレイアウトが縦並びに変わる
- 画面が読みやすく、避難情報がすぐにわかる

---

## 実行時の注意

この指示は「今のホーム画面を正確に再現すること」を目的としています。見た目や挙動の再現性を重視してください。完全な一致を目指しつつ、実際のアプリの文脈に対して自然で使いやすい UI にしてください。

ホーム画面は、避難情報を最優先に可視化するためのダッシュボードであることを常に意識してください。

---

## 一言でまとめると

「青森市の防災情報を一覧できるホームダッシュボードを、情報の見やすさと避難判断のしやすさを最優先にした構成で実装し、マップ・警報・お知らせ・更新・検索・現在地・レスポンシブ対応を含む完全なホーム画面仕様として再現せよ。」
